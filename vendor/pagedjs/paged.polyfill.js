/* Placeholder to keep static structure; you can drop in the paged.polyfill.js file here for fully offline use.
   For now, printing will fall back to the browser if polyfill not present. */
window.Paged = window.Paged || undefined;


